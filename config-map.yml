apiVersion: v1
kind: ConfigMap
metadata:
  namespace: gob-gab
  name: nginx-config
data:
  default.conf: | 
    server {
      listen 80;
      server_name_;
      location / {
        return 200 "Monchavin Pinthong";
      }
    }